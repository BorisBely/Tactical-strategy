/// <summary>
/// Subjective threat band. Independent from <see cref="PerceivedRelationship"/>:
/// Hostile + far can be Low. Does not decide fire (G6).
/// </summary>
public enum ThreatLevel
{
	None = 0,
	Low = 1,
	Medium = 2,
	High = 3
}
